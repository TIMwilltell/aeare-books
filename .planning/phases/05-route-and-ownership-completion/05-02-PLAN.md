---
phase: 05-route-and-ownership-completion
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/lib/api/export.ts
  - src/lib/db/index.ts
  - src/routes/book/[id]/+page.svelte
  - tests/e2e/route-and-ownership.spec.ts
autonomous: true
requirements: [AUTH-04, ACL-02, DATA-02]
must_haves:
  truths:
    - Signed-in users can see owned progress on the book detail page and signed-out users still cannot reach protected routes.
    - Library export contains only the current user's books and progress events.
    - A second signed-in user does not inherit the first user's shelf or progress export data.
  artifacts:
    - path: src/routes/book/[id]/+page.svelte
      provides: Protected book-detail progress timeline bound to the current user's book.
    - path: src/lib/api/export.ts
      provides: Account-scoped export payload including books and progress events.
    - path: tests/e2e/route-and-ownership.spec.ts
      provides: Browser proof for two-user route/data isolation and export contents.
  key_links:
    - from: src/routes/book/[id]/+page.svelte
      to: src/lib/db/index.ts
      via: `getProgressEventsByBook(book.id)`
      pattern: getProgressEventsByBook
    - from: src/lib/api/export.ts
      to: src/convex/progress.ts
      via: `api.progress.getAll`
      pattern: progress.getAll
    - from: tests/e2e/route-and-ownership.spec.ts
      to: src/lib/api/export.ts
      via: downloaded JSON assertions
      pattern: progressEvents
---

<objective>
Wire owned progress into the live UI and export flows so Phase 5 proves scoped data behavior end-to-end.

Purpose: close the remaining user-visible DATA-02 / ACL-02 gap by showing current-user progress on protected routes and exporting the same scoped records.
Output: protected progress timeline, complete account-scoped export payload, and browser regression evidence for route/data isolation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-route-and-ownership-completion/05-RESEARCH.md
@.planning/phases/05-route-and-ownership-completion/05-VALIDATION.md
@.planning/phases/04-auth-session-closure/04-01-SUMMARY.md
@src/lib/db/index.ts
@src/lib/api/export.ts
@src/routes/book/[id]/+page.svelte
@src/lib/components/ProgressTimeline.svelte
@tests/e2e/auth-route-guards.spec.ts
@tests/e2e/helpers/auth.ts

<interfaces>
From `src/lib/db/index.ts`:
```ts
export interface Book { id: string; title: string; author: string; ... }
export interface ProgressEvent {
  id: string;
  bookId: string;
  eventType: 'marked_read' | 'quiz_completed' | 'notes_added' | 'book_added';
  value?: string;
  eventDate: number;
  createdAt: number;
}
export async function getBook(id: string): Promise<Book | null>;
export async function getProgressEventsByBook(bookId: string): Promise<ProgressEvent[]>;
```

From `src/lib/api/export.ts`:
```ts
export interface ExportData {
  exportedAt: string;
  version: '1.0';
  books: any[];
  progressEvents: any[];
}
export async function exportLibrary(): Promise<void>;
```

From `tests/e2e/helpers/auth.ts`:
```ts
export async function signInWithLocalInbox(page: Page, request: APIRequestContext, email: string);
export async function clearMagicLinks(request: APIRequestContext, email: string);
export const protectedRoutes = ['/scan', '/book/new', '/book/test-book-id'];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Show owned progress on the protected detail page</name>
  <files>src/lib/db/index.ts, src/routes/book/[id]/+page.svelte</files>
  <read_first>src/lib/db/index.ts, src/routes/book/[id]/+page.svelte, src/lib/components/ProgressTimeline.svelte, src/convex/progress.ts</read_first>
  <behavior>
    - Test 1: opening an owned book detail page loads progress events for that book and renders the existing timeline component.
    - Test 2: a freshly added book shows the backend-created `book_added` event in the detail flow.
    - Test 3: if progress loading fails, the detail page stays usable and surfaces a visible non-crashing error state.
  </behavior>
  <action>Extend `src/routes/book/[id]/+page.svelte` with dedicated progress state: `progressEvents`, `progressLoading`, and `progressError`. After `getBook(bookId)` resolves, call `getProgressEventsByBook(book.id)` from `src/lib/db/index.ts`; render `ProgressTimeline` in the detail layout when events load, keep the page usable while the timeline loads, and show a visible section-card error if the protected progress query fails. Reuse the existing `ProgressEvent` interface and `ProgressTimeline.svelte`; do not invent a second timeline component or fall back to mock data.</action>
  <acceptance_criteria>
    - `src/routes/book/[id]/+page.svelte` imports `ProgressTimeline` and `getProgressEventsByBook`.
    - The detail page contains a rendered progress section for loaded books and a visible error state for progress-load failure.
    - No progress data is read from local mock arrays; the page uses the protected Convex-backed helper.
  </acceptance_criteria>
  <verify>
    <automated>bun run check</automated>
  </verify>
  <done>Owned book detail pages display real protected progress data through the existing timeline component without breaking the rest of the page when the progress query fails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export owned progress and prove two-user isolation in Playwright</name>
  <files>src/lib/api/export.ts, tests/e2e/route-and-ownership.spec.ts</files>
  <read_first>src/lib/api/export.ts, src/lib/db/index.ts, src/convex/books.ts, src/convex/progress.ts, tests/e2e/auth-route-guards.spec.ts, tests/e2e/helpers/auth.ts</read_first>
  <behavior>
    - Test 1: user A adds a book, exports the library, and the downloaded JSON contains exactly user A's book plus at least one owned progress event for that book.
    - Test 2: after signing out and signing in as user B, the shelf is empty (or excludes user A's data) and the exported JSON contains no user A book or progress rows.
    - Test 3: signed-out route protection still redirects `/scan`, `/book/new`, and `/book/test-book-id` to `/`.
  </behavior>
  <action>Update `src/lib/api/export.ts` to query both `api.books.getAll` and `api.progress.getAll` from the authenticated browser Convex client, then serialize `createdAt`, `eventDate`, `readDate`, and `quizDate` to ISO strings in the downloaded JSON. Add `tests/e2e/route-and-ownership.spec.ts` that uses the local inbox helpers to sign in as two separate users, creates a book through the live UI for user A, verifies the book detail timeline shows `Book added to library`, downloads and parses the JSON export, and asserts that user B cannot see or export user A's records. Reuse `protectedRoutes` coverage in the new suite or call the shared redirect helper so AUTH-04 evidence lives in the Phase 5 browser artifact as well.</action>
  <acceptance_criteria>
    - `src/lib/api/export.ts` queries `api.progress.getAll` and no longer hardcodes `progressEvents: []`.
    - `tests/e2e/route-and-ownership.spec.ts` exists and contains assertions for two different signed-in users plus parsed export JSON checks.
    - The new browser suite asserts signed-out redirects for the protected routes covered by Phase 5.
  </acceptance_criteria>
  <verify>
    <automated>bun run check && bunx playwright test tests/e2e/route-and-ownership.spec.ts</automated>
  </verify>
  <done>Users can export both books and progress for only their own account, the detail page shows owned progress, and browser tests prove route/data isolation across two separate accounts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protected browser UI → Convex queries | The client may be signed out, stale, or belong to a different account than previously loaded data |
| export download → local file | Downloaded JSON must not include another account's library data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | I | `src/routes/book/[id]/+page.svelte` | mitigate | Load progress only through `getProgressEventsByBook(book.id)` so the page inherits the server-side ownership gate instead of reusing stale client cache or mock data. |
| T-05-05 | I | `src/lib/api/export.ts` | mitigate | Export through authenticated `api.books.getAll` and `api.progress.getAll` only, then prove in Playwright that a second account cannot see the first account's export rows. |
| T-05-06 | D | detail progress load | accept | A failed progress query may hide the timeline section, but the page remains usable and shows a visible retry-safe error; acceptable low-severity availability impact. |

</threat_model>

<verification>
Run `bun run check` and `bunx playwright test tests/e2e/route-and-ownership.spec.ts`. Confirm the suite parses the downloaded JSON and proves two-user isolation for books and progress data.
</verification>

<success_criteria>
- Protected book detail pages render owned progress data for the signed-in account.
- Library export payloads include progress events and remain scoped to the authenticated user.
- Browser regression evidence demonstrates both signed-out route protection and two-user data isolation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-route-and-ownership-completion/05-02-SUMMARY.md`
</output>
