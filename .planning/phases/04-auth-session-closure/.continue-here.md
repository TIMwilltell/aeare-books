---
context: phase
phase: 04-auth-session-closure
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-04-09T03:34:42.947Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Treating local Convex identity tests as full Phase 4 closure | `src/convex/users.test.ts` proves server-side identity stability, but it does not replace the dedicated browser-level first-sign-in/repeat-sign-in proof required by `04-02-PLAN.md`. | advisory | Do not mark DATA-01 done until Playwright observes the authenticated current-user mapping through an app-safe, server-derived surface. |
| Inferring AUTH-02 completion from reload coverage alone | Reload/session restore now passes, but refresh-token failure desync is still missing explicit end-to-end evidence. | advisory | Add a deterministic refresh-failure test path and keep `04-VERIFICATION.md` partial until that path passes. |

<current_state>
Active context is Phase 4: `04-auth-session-closure`.

Plan 1 is mostly closed from the browser side: home Scan CTA redirect, reload/session restore, and sign-out reset now have passing Playwright coverage in `tests/e2e/auth-route-guards.spec.ts`, and the evidence is recorded in `04-VERIFICATION.md`.

Plan 2 is still in progress. This session added a local Convex test harness (`vitest.config.ts`, `src/convex/test.setup.ts`, `src/convex/users.test.ts`) and updated `README.md` plus `04-VERIFICATION.md`, but the phase remains partial because there is still no dedicated browser-level identity-mapping spec and no explicit refresh-token failure proof.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Restore home-entry, reload, and sign-out auth-session coverage - Done
- Task 2: Add local Convex identity-mapping harness - Done
- Task 3: Update Phase 4 verification artifact and rerun docs - In progress; artifact exists, but requirement closure is still partial
</completed_work>

<remaining_work>

- Task 1: Implement a safe browser-visible assertion path for current-user identity mapping without introducing any client-provided identity key
- Task 2: Add `tests/e2e/auth-session-closure.spec.ts` covering first sign-in, sign-out, repeat sign-in, and reload with stable internal user mapping assertions
- Task 3: Add deterministic refresh-token failure end-to-end proof so AUTH-02 can move from partial to satisfied
- Task 4: Finish `.planning/phases/04-auth-session-closure/04-01-SUMMARY.md` and `04-02-SUMMARY.md` after the missing evidence lands
</remaining_work>

<decisions_made>

- Added local Convex tests first because they prove server-derived identity behavior safely and reduce uncertainty before exposing any browser-visible verification surface
- Kept `04-VERIFICATION.md` explicitly partial instead of over-claiming completion from indirect evidence
- Preserved the no-client-supplied-identity rule from `04-02-PLAN.md`; any resumed work must continue to anchor identity to `getAuthUserId(ctx)`-derived behavior
</decisions_made>

<blockers>
- Technical: There is still no browser-visible, app-safe assertion path for current-user identity mapping and refresh-failure behavior, so Phase 4 cannot be closed yet.
- Worktree note: The repo has unrelated concurrent edits outside this phase handoff. Resume without reverting or reshaping those changes.
</blockers>

## Required Reading (in order)
1. `src/convex/_generated/ai/guidelines.md` - repo-specific Convex rules that must constrain any resumed backend/test-harness work
1. `.planning/phases/04-auth-session-closure/04-02-PLAN.md` - defines the remaining must-haves for identity mapping and verification closure
1. `.planning/phases/04-auth-session-closure/04-01-PLAN.md` - refresh-failure proof is still a remaining gap from Plan 1
1. `.planning/phases/04-auth-session-closure/04-VERIFICATION.md` - current evidence and exact partial-status wording to preserve or update
1. `src/convex/users.test.ts` - current local identity-mapping proof and the server-side behavior already covered
1. `README.md` - local rerun instructions were updated and should stay aligned with any new verification path

## Critical Anti-Patterns (do NOT repeat these)
- Over-closing Phase 4 from indirect evidence only -> require direct Playwright proof for refresh-failure handling and browser-visible identity mapping before writing summaries
- Adding any test helper that accepts a client-provided `userId` -> keep all identity resolution server-derived from the authenticated session

## Infrastructure State
- No known background processes were captured as part of this pause
- Browser reruns will require the usual local app/auth environment used by the existing Playwright local inbox flow
- `bun run test:convex` is now available for local Convex-only verification

<context>
The main thing to preserve is that the session made real progress but did not finish the phase. The auth route-guard flow is substantially stronger now, parity coverage still passes after AR cleanup, and the local Convex tests prove repeat-session identity stability. The remaining work is narrower than before: add direct browser proof for refresh failure and user mapping, then close the summaries.
</context>

<next_action>
Start with: design the smallest app-safe observable for current authenticated user identity (or equivalent test-only harness), then implement `tests/e2e/auth-session-closure.spec.ts` around that surface before touching summaries.
</next_action>
