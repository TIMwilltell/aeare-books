---
phase: 04-auth-session-closure
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/convex/users.ts
  - tests/e2e/auth-session-closure.spec.ts
  - README.md
  - .planning/phases/04-auth-session-closure/04-VERIFICATION.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, DATA-01]
must_haves:
  truths:
    - First sign-in creates or resolves the authenticated internal user record successfully.
    - Repeat sign-in for the same identity resolves to the same internal user record rather than duplicating or drifting.
    - Phase 4 ships with concrete verification evidence for sign-in, reload/session restore, and sign-out.
  artifacts:
    - path: src/convex/users.ts
      provides: Auth-derived current-user query surface used to verify stable internal user mapping.
    - path: tests/e2e/auth-session-closure.spec.ts
      provides: Browser automation for identity mapping and full auth-session closure flows.
    - path: .planning/phases/04-auth-session-closure/04-VERIFICATION.md
      provides: Recorded evidence and rerun commands for the audit gate.
    - path: README.md
      provides: Updated operator instructions for running the Phase 4 auth verification suite.
  key_links:
    - from: tests/e2e/auth-session-closure.spec.ts
      to: src/convex/users.ts
      via: authenticated user identity assertions
      pattern: getCurrentUser
    - from: README.md
      to: .planning/phases/04-auth-session-closure/04-VERIFICATION.md
      via: documented verification commands and recovery notes
      pattern: test:e2e
---

<objective>
Prove the auth stack maps identities correctly and leave a reusable verification artifact for the Phase 4 audit gate.

Purpose: turn the remaining blocked auth/session checks into executable, recorded evidence that closes AUTH-01/02/03 and DATA-01 for this phase.
Output: identity-mapping assertions, Phase 4 verification notes, and updated run instructions for future reruns.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/STATE.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@.planning/workstreams/milestone/phases/01-auth-foundation/01-02-SUMMARY.md
@.planning/workstreams/milestone/phases/01-auth-foundation/01-03-SUMMARY.md
@src/convex/users.ts
@tests/e2e/helpers/auth.ts
@README.md

<interfaces>
From `src/convex/users.ts`:
```ts
export const getCurrentUser = query({ args: {}, returns: userResultValidator, handler: async (ctx) => ... });
export const ensureCurrentUser = query({ args: {}, returns: userResultValidator, handler: async (ctx) => ... });
```

From `tests/e2e/helpers/auth.ts`:
```ts
export async function clearMagicLinks(request: APIRequestContext, email: string);
export async function signInWithLocalInbox(page: Page, request: APIRequestContext, email: string);
export function rewriteMagicLinkForLocalApp(url: string, currentPageUrl: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stable identity-mapping verification coverage</name>
  <files>src/convex/users.ts, tests/e2e/auth-session-closure.spec.ts</files>
  <behavior>
    - Test 1: First local magic-link sign-in resolves a non-null internal user record for the authenticated session.
    - Test 2: Signing out and signing back in with the same email resolves the same internal user identity again.
    - Test 3: Reloading between sign-in attempts keeps the resolved internal user record stable.
  </behavior>
  <action>Extend the authenticated user query surface only as needed to support safe identity assertions without ever accepting a client-provided user ID. Add a dedicated Playwright spec that signs in through the local inbox, inspects the authenticated current-user mapping through app-safe behavior or a tightly scoped test-only harness, signs out, signs back in with the same email, and proves the internal user record is stable across first-use, repeat sign-in, and reload. Keep all authorization decisions server-derived via `getAuthUserId(ctx)` per the established auth contract.</action>
  <verify>
    <automated>bunx convex codegen && bun run check && bunx playwright test tests/e2e/auth-session-closure.spec.ts</automated>
  </verify>
  <done>Identity mapping has automated first-use and repeat-sign-in proof, the test only observes auth-derived state, and no client-supplied identity key is introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Record audit-ready Phase 4 verification evidence</name>
  <files>.planning/phases/04-auth-session-closure/04-VERIFICATION.md, README.md</files>
  <action>Write Phase 4 verification notes that capture the exact commands run, the auth flows covered, any required local auth preconditions, and the resulting evidence for AUTH-01, AUTH-02, AUTH-03, and DATA-01. Update `README.md` so contributors know how to rerun the Phase 4 auth-session suite locally using the built-in local inbox flow and where to find the recorded verification artifact. Treat this as milestone evidence, not generic docs: include the sign-in, reload/session restore, sign-out, and repeat-sign-in mapping checks explicitly.</action>
  <verify>
    <automated>test -f .planning/phases/04-auth-session-closure/04-VERIFICATION.md && bunx playwright test tests/e2e/auth-session-closure.spec.ts</automated>
  </verify>
  <done>The repo contains a Phase 4 verification artifact with explicit auth-session evidence and the README points future operators at the exact rerun path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser auth session → Convex user query | Authenticated client session requests server-derived identity information |
| repo verification docs → future operators | Human reruns depend on the accuracy of recorded commands and auth prerequisites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | S | `src/convex/users.ts` | mitigate | Keep identity resolution anchored to `getAuthUserId(ctx)` and do not add any client-provided `userId` argument or alternate lookup key. |
| T-04-06 | T | `tests/e2e/auth-session-closure.spec.ts` | mitigate | Any test-only harness must read current-user identity through authenticated app/Convex surfaces and must not patch ownership data directly. |
| T-04-07 | R | `.planning/phases/04-auth-session-closure/04-VERIFICATION.md` | mitigate | Record exact commands, timestamps/results, and requirement mapping so future audits can trace what was actually demonstrated. |
| T-04-08 | I | verification artifact + README | accept | Verification docs mention auth env prerequisites already required for local development; no secrets should be written, and low-risk setup references are acceptable. |

</threat_model>

<verification>
Run `bunx convex codegen`, `bun run check`, and the dedicated Phase 4 Playwright spec. Confirm `.planning/phases/04-auth-session-closure/04-VERIFICATION.md` maps evidence to AUTH-01, AUTH-02, AUTH-03, and DATA-01.
</verification>

<success_criteria>
- Repeat sign-in resolves the same internal user mapping as first sign-in.
- Phase 4 has a committed verification artifact covering sign-in, reload/session restore, sign-out, and identity mapping.
- README tells contributors how to rerun the Phase 4 auth verification suite with local inbox auth.
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-session-closure/04-02-SUMMARY.md`
</output>
