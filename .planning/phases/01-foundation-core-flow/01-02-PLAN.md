---
phase: 01-foundation-core-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: true
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-03
  - SCAN-04
  - LIB-01
  - LIB-02
  - LIB-03
  - LIB-04
  - PWA-01

must_haves:
  truths:
    - "User can scan barcode on iOS Safari"
    - "User can scan barcode on Android Chrome"
    - "User can manually enter ISBN when camera unavailable"
    - "User can view library and search/filter by title or author"
    - "User can view individual book details"
    - "User can delete a book from library with confirmation"
    - "App works offline"
  artifacts:
    - path: "src/lib/db/index.ts"
      provides: "Dexie database with Book CRUD"
    - path: "src/lib/components/Scanner.svelte"
      provides: "Quagga2 barcode scanner"
    - path: "src/routes/+page.svelte"
      provides: "Library view with search and FAB"
    - path: "src/routes/book/new/+page.svelte"
      provides: "Book form with cancel button"
    - path: "src/routes/book/[id]/+page.svelte"
      provides: "Book detail view"
    - path: "src/lib/components/DeleteDialog.svelte"
      provides: "Delete confirmation dialog"
  key_links:
    - from: "Scanner.svelte"
      to: "/book/new"
      via: "navigate with ISBN param"
      pattern: "book/new.*isbn="
    - from: "+page.svelte"
      to: "db.searchBooks()"
      via: "search bar input"
      pattern: "searchBooks"
    - from: "BookList.svelte"
      to: "/book/[id]"
      via: "row click"
      pattern: "book/.*"
---

<objective>
**Scanner + Library Management**

Implement barcode scanning with Quagga2, Dexie.js for IndexedDB, and library CRUD. Users can scan books, manage their library offline.

Purpose: Core scanning workflow and data persistence
Output: Scanner component, library view, book form, delete flow
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 1 success criteria: scan, view library, delete, install)
@.planning/phases/01-foundation-core-flow/01-CONTEXT.md (D-01 to D-08 — user decisions)
@.planning/research/STACK.md (Quagga2, Dexie.js)

**Locked Decisions from CONTEXT.md:**
- D-01: Books displayed as compact list view
- D-02: Each row shows: Title + Author + AR level/points
- D-03: After scan → full-screen form page (navigates away from scanner)
- D-04: Form has explicit Cancel button
- D-05: FAB for scan action — overlays library view
- D-06: Top search bar for library filtering (persistent)
- D-07: Empty library shows friendly prompt with Scan button
- D-08: Delete shows confirmation dialog
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Dexie.js database schema</name>
  <files>src/lib/db/index.ts, src/lib/db/schema.ts</files>
  <read_first>
  - .planning/research/STACK.md — confirms Dexie.js 4.x recommendation
  - .planning/research/SUMMARY.md — confirms IndexedDB as source of truth
  </read_first>
  <action>
    Install Dexie.js: dexie: ^4.0.0
    
    Create src/lib/db/index.ts:
    - Define Book interface with: id (auto-increment), isbn, title, author, 
      coverUrl (optional), arLevel (optional), arPoints (optional), 
      arDataSource (enum: 'fetched' | 'manual'), createdAt, updatedAt
    - Define Dexie class extending Dexie:
      - books: Table<Book, number>
      - syncQueue: Table<SyncQueueItem, number>
    - Export db instance
    - Implement addBook(title, author, isbn, coverUrl?), updateBook(id, data), 
      deleteBook(id), getAllBooks(), searchBooks(query)
    
    Create sync queue table for future offline sync:
    - Operation enum: 'create' | 'update' | 'delete'
    - SyncQueueItem: id, operation, tableName, recordId, data, createdAt, status
  </action>
  <verify>
    <automated>grep -q "class Book" src/lib/db/index.ts && grep -q "dexie" package.json</automated>
  </verify>
  <done>
    Dexie database with books table, CRUD operations, search implemented
  </done>
</task>

<task type="auto">
  <name>Task 2: Build scanner component with Quagga2</name>
  <files>src/lib/components/Scanner.svelte, src/routes/scan/+page.svelte</files>
  <read_first>
  - .planning/research/SUMMARY.md — confirms Quagga2 for barcode scanning
  - .planning/phases/01-foundation-core-flow/01-CONTEXT.md — D-03: navigate to form after scan
  </read_first>
  <action>
    Install Quagga2: quagga: ^2.1.0
    
    Create src/lib/components/Scanner.svelte:
    - Use onMount to initialize Quagga with:
      - inputStream: type: "LiveStream", target: video element, constraints: 
        facingMode: "environment", width: { min: 640, ideal: 1280, max: 1920 },
        height: { min: 480, ideal: 720, max: 1080 }
      - locator: patchSize: "medium", halfSample: true
      - decoder: readers: ["ean_reader", "ean_8_reader", "upc_reader", "upc_e_reader"]
      - locate: true
    - Handle onDetected callback to extract ISBN from result
    - Show scanning status indicator (scanning, found, error)
    - Handle camera permission denied gracefully
    - Emit event with scanned ISBN
    
    Create src/routes/scan/+page.svelte:
    - Mount Scanner component
    - On ISBN detected: navigate to /book/new?isbn={isbn}
    - Show "Use camera" button to trigger scanner
    - Show "Enter manually" link to /book/new (manual entry fallback per SCAN-04)
  </action>
  <verify>
    <automated>grep -q "quagga" package.json && ls src/lib/components/Scanner.svelte</automated>
  </verify>
  <done>
    Camera scanner works on iOS Safari and Android Chrome, manual entry fallback available
  </done>
</task>

<task type="auto">
  <name>Task 3: Build book form with cancel button</name>
  <files>src/routes/book/new/+page.svelte, src/routes/book/[id]/+page.svelte</files>
  <read_first>
  - .planning/phases/01-foundation-core-flow/01-CONTEXT.md — D-03: navigate to form, D-04: cancel button
  - src/lib/db/index.ts — Book interface
  </read_first>
  <action>
    Create src/routes/book/new/+page.svelte:
    - Check URL param ?isbn= for pre-filled ISBN
    - Form fields: ISBN (pre-filled if provided), Title, Author
    - Save button calls db.addBook()
    - Cancel button navigates back to / (library)
    - Use Svelte 5 runes for form state
    
    Create src/routes/book/[id]/+page.svelte:
    - Load book by id from db
    - Display book details: Title, Author, ISBN, Cover (if available)
    - Edit button to edit mode
    - Delete button triggers confirmation dialog per D-08
  </action>
  <verify>
    <automated>ls src/routes/book/new/ && ls src/routes/book/\[id\]/</automated>
  </verify>
  <done>
    Book form navigates from scanner, save adds to IndexedDB, cancel returns to library
  </done>
</task>

<task type="auto">
  <name>Task 4: Build library view with search and FAB</name>
  <files>src/routes/+page.svelte, src/lib/components/BookList.svelte, src/lib/components/FAB.svelte</files>
  <read_first>
  - .planning/phases/01-foundation-core-flow/01-CONTEXT.md — D-01, D-02, D-05, D-06, D-07
  - src/lib/db/index.ts — searchBooks function
  </read_first>
  <action>
    Create src/routes/+page.svelte:
    - Top search bar: input field with on:input filter, bound to searchBooks()
    - FAB: fixed position bottom-right, navigates to /scan
    - Render BookList component with filtered books
    - Empty state: show friendly prompt with Scan button per D-07
    
    Create src/lib/components/BookList.svelte:
    - Receive books array as prop
    - Render compact list view per D-01
    - Each row: Title + Author + AR level/points (when available) per D-02
    - Row click navigates to /book/{id} per LIB-03
    
    Create src/lib/components/FAB.svelte:
    - Fixed position bottom-right
    - Icon: camera or plus
    - On click: navigate to /scan
  </action>
  <verify>
    <automated>grep -q "FAB" src/routes/+page.svelte && grep -q "search" src/routes/+page.svelte</automated>
  </verify>
  <done>
    Library view shows books in list format, search filters in real-time, FAB opens scanner
  </done>
</task>

<task type="auto">
  <name>Task 5: Implement delete with confirmation</name>
  <files>src/lib/components/DeleteDialog.svelte</files>
  <read_first>
  - .planning/phases/01-foundation-core-flow/01-CONTEXT.md — D-08: confirmation dialog
  - src/routes/book/[id]/+page.svelte — delete button
  </read_first>
  <action>
    Create src/lib/components/DeleteDialog.svelte:
    - Props: bookTitle (string), open (boolean)
    - Shows dialog: "Delete {title}?" with Cancel and Delete buttons
    - On Cancel: emit close event
    - On Delete: call db.deleteBook(id), navigate to library
    
    Integrate into book detail page per LIB-04
  </action>
  <verify>
    <automated>ls src/lib/components/DeleteDialog.svelte</automated>
  </verify>
  <done>
    Delete shows confirmation, removes book from library
  </done>
</task>

</tasks>

<verification>
- [ ] Scanner works on iOS Safari and Android Chrome
- [ ] Manual ISBN entry works when camera unavailable (SCAN-04)
- [ ] Library displays books in list view with title/author
- [ ] Search filters library in real-time (LIB-02)
- [ ] Book detail view shows all info (LIB-03)
- [ ] Delete shows confirmation, removes book (LIB-04)
- [ ] FAB navigates to scanner
- [ ] Empty library shows friendly prompt
- [ ] Data persists in IndexedDB (works offline - PWA-01)
</verification>

<success_criteria>
- User can scan barcode on iOS Safari (SCAN-02) and Android Chrome (SCAN-03)
- User can manually enter ISBN when camera unavailable (SCAN-04)
- User can view library and search/filter by title or author (LIB-01, LIB-02)
- User can view individual book details (LIB-03)
- User can delete a book from library with confirmation (LIB-04)
- App works offline (PWA-01)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-flow/01-02-SUMMARY.md`
</output>
