---
phase: 01-foundation-core-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - PWA-03

must_haves:
  truths:
    - "App can be installed to phone home screen"
    - "PWA manifest includes required installability features"
    - "Build completes without errors"
  artifacts:
    - path: "package.json"
      provides: "SvelteKit + vite-plugin-pwa dependencies"
    - path: "vite.config.ts"
      provides: "PWA plugin configuration"
    - path: "src/app.html"
      provides: "PWA manifest link, iOS meta tags"
    - path: "src/routes/+page.svelte"
      provides: "App entry point"
  key_links:
    - from: "src/app.html"
      to: "/manifest.webmanifest"
      via: "link tag"
      pattern: "rel=\"manifest\""
---

<objective>
**SvelteKit + PWA Shell Setup**

Create the SvelteKit project with PWA configuration. Users can install the app to their phone home screen.

Purpose: Foundation for mobile-first PWA, enables offline capability
Output: SvelteKit project with vite-plugin-pwa, installable manifest
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 1 goal: users can scan books and manage library offline)
@.planning/STATE.md
@.planning/phases/01-foundation-core-flow/01-CONTEXT.md (D-01 to D-08 — user decisions)
@.planning/research/STACK.md (SvelteKit, vite-plugin-pwa)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize SvelteKit project</name>
  <files>package.json, svelte.config.js, vite.config.ts, tsconfig.json</files>
  <read_first>
  - .planning/research/STACK.md — confirms SvelteKit 2.x recommendation
  - .planning/PROJECT.md — confirms PWA architecture requirement
  </read_first>
  <action>
    Initialize a new SvelteKit project with:
    - Svelte 5 (runes)
    - TypeScript
    - @sveltejs/adapter-auto for deployment flexibility
    
    Install dependencies:
    - svelte: ^5.0.0
    - @sveltejs/kit: ^2.0.0
    - @sveltejs/adapter-auto: ^3.0.0
    - vite: ^5.0.0
    - typescript: ^5.0.0
  </action>
  <verify>
    <automated>npm --version && node --version && cat package.json | grep -q '"@sveltejs/kit"'</automated>
  </verify>
  <done>
    SvelteKit project initialized with TypeScript, npm install completes without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add PWA configuration</name>
  <files>vite.config.ts, src/app.html</files>
  <read_first>
  - .planning/research/SUMMARY.md — confirms vite-plugin-pwa recommendation
  - .planning/REQUIREMENTS.md — PWA-03: app can be installed to home screen
  </read_first>
  <action>
    Install vite-plugin-pwa: ^0.22.0
    
    Configure vite.config.ts with PWA plugin:
    - manifest: name "AeAre Books", short_name "AeAre", theme_color "#4A90D9"
    - icons: 192x192, 512x512 (placeholder SVG generated)
    - start_url: "/"
    - display: "standalone"
    - orientation: "portrait"
    - scope: "/"
    - strategy: "generateSW" (simple service worker generation)
    - workbox: globPatterns for app shell caching
    
    Add manifest link to src/app.html:
    `<link rel="manifest" href="/manifest.webmanifest" />`
    
    Configure iOS meta tags in app.html:
    - apple-mobile-web-app-capable: yes
    - apple-mobile-web-app-status-bar-style: default
    - apple-mobile-web-app-title: AeAre Books
  </action>
  <verify>
    <automated>grep -q "vite-plugin-pwa" package.json && ls src/app.html</automated>
  </verify>
  <done>
    PWA manifest generated at /manifest.webmanifest, service worker registered, app installable to home screen
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify build and dev server</name>
  <files>src/routes/+page.svelte</files>
  <action>
    Create a minimal +page.svelte with:
    - Basic page title "AeAre Books"
    - Simple heading "Loading..."
    
    Run `npm run build` to verify compilation
    
    Run `npm run dev` (background, check it starts without port conflicts)
  </action>
  <verify>
    <automated>npm run build exits 0</automated>
  </verify>
  <done>
    Build completes successfully, no TypeScript errors
  </done>
</task>

</tasks>

<verification>
- [ ] SvelteKit project runs without errors
- [ ] PWA manifest generated correctly
- [ ] Build completes without errors
</verification>

<success_criteria>
- App can be installed to phone home screen (PWA-03)
- PWA manifest includes required installability features
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-flow/01-01-SUMMARY.md`
</output>
